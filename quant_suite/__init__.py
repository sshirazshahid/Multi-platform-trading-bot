"""quant_suite - validated strategy research + ensemble engine for the bot.
Self-contained (ccxt/pandas/numpy). Generates signals/backtests only; never
places orders. See ensemble.py for the integrated multi-strategy engine and
bot_adapter.py for the paper-safe bot integration."""
__version__ = "0.2.0"
