ccxt==4.5.64
pandas==3.0.3
numpy==2.5.1
python-dotenv==1.2.2
loguru==0.7.3
rich==15.0.0
websockets==16.0
schedule==1.2.2
colorama==0.4.6
tabulate==0.10.0
requests>=2.31.0
pyyaml>=6.0
jsonschema>=4.25.1
# 2026-05-03: psutil — heartbeat memory_mb tracking (bot_engine.py:2217).
# Without it, heartbeat reports memory_mb=0 forever, masking leaks.
psutil>=5.9.0

# Read-only local warehouse MCP server (mcp_server/trading_bot_mcp.py).
mcp==1.28.1

# Quant ML stack (Phase 0+ of quant rebuild plan)
scikit-learn==1.9.0
scipy==1.18.0
statsmodels==0.14.6
arch==8.0.0
hmmlearn==0.3.3
joblib==1.5.3

# Mission Control WebUI (localhost full-ops console)
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
httpx>=0.27.0

# Testing
pytest==9.1.1
pytest-cov==7.1.0
