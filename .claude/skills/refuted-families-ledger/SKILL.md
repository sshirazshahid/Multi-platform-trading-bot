---
name: refuted-families-ledger
description: The binding ledger of strategy families this trading bot has ALREADY refuted with pre-registered gates, the one validated family, and the evidence bar to reopen anything. MUST be consulted before proposing, researching, screening, backtesting, or implementing ANY trading strategy or edge idea — including requests phrased as "new strategy", "apply strategies", "trading patterns", "what should the bot trade", "add an indicator", or re-runs of old ideas. If a strategy request arrives, read this FIRST.
---

# Refuted-Families Ledger

~2,400+ pattern tests were run on this system's own 3-year multi-venue data with pre-registered gates. Re-litigating them wastes money and risks −EV trades for an owner who depends on this capital. These verdicts are binding until the reopen bar is met — that is why this ledger exists as a file instead of folklore.

## Validated (the only family with a live pipeline)
Delta-neutral cross-venue funding-rate carry (**F1**): 15 coins, maker-first execution, in PAPER soak. External support: peer-reviewed ScienceDirect 2025 study (60 scenarios, ≤115.9%/6mo, max loss 1.92%, zero HODL correlation). Extensions — cross-venue funding *dispersion*, settlement-window timing — are ADJACENT and screen-eligible.

## In shadow (log-only — hypotheses ON TRIAL, not validated; pipeline CONFIRMED-GO unless a row says otherwise)
| Family | Registration + binding fragility profile | Date |
|---|---|---|
| Textbook breakout — Codex deep-run winner breakout_60d (BTC/ETH/SOL/XRP/BNB/ADA/DOGE/LINK/AVAX/DOT bybit perps, 4h; close beyond the shifted prior-60d high/low channel, 2.2×ATR(14) stop, 3R target, 21d max hold, notational 1%-risk sizing) | **Owner-directed forward paper probe — NOT a pipeline GO; the reopen bar is NOT met.** The family stays in the Refuted table below (textbook trend/breakout 0/40 OOS, 2026-06-13; donchian_breakout scored F in Codex's own first 6-month sweep) — this row does NOT reopen it. The Codex deep run (`reports/DEEP_FUTURES_RESEARCH.md`, 2026-07-11) is the family's strongest external evidence yet — 5-6yr × 10 markets, survives 2× costs (100% cost survival), 9/9 parameter-neighborhood cells stable around the confirmed (2.2, 3.0) spec — BUT (a) the winner was selected ON holdout metrics across 20 candidates (holdout burned; a flat 6-point trial penalty is not DSR/PBO multiplicity control) and (b) **Codex's OWN Monte Carlo fails our frozen capital-preservation gates: P(positive) 91.5% < 0.95 floor, maxDD p95 42.5% > 0.25 cap.** ⚠ WR is ~30-35% BY DESIGN (3:1 R:R trend system; per-market backtest WR 23-41%) — conflicts with the owner's standing ≥65% WR-floor preference, so even a gate-passing result could never enter the accuracy-band lane without a separate explicit owner decision. Probe: `core/agents/breakout_probe_agent.py`, arm `breakout_60d_4h_v1`, frozen score tanh(penetration/0.02) (scale from the Codex cache distribution, median 0.0137, n=1,604) never re-tuned post-outcome. Expectation is NO-PROMOTE; promotion (only if forward evidence surprises) via frozen `core/promotion_gate.py` on ≥30 RESOLVED forward events + explicit owner sign-off. Probe exists because Codex's own creation gate requires forward paper trading and the owner directed implementing that recommendation | 2026-07-11 |
| Time-series momentum — Codex TSMOM-20d, 1h + 4h arms (BTC/ETH/SOL bybit perps; 20d momentum sign + 5d-EMA side filter, 2×ATR(14) stop, 2R target, 7d max hold, notational 1%-risk sizing) | **Owner-directed forward paper probe — NOT a pipeline GO; the reopen bar is NOT met.** TSMOM stays in the Refuted table below (long-only TSMOM: no profit edge, 2026-06-15; textbook trend/breakout 0/40 OOS, 2026-06-13) — this row does NOT reopen the family. The external Codex backtest that recommended "regime-watch: monitor in paper mode, do NOT automate" (bot_weight 0.0) is ~30 OOS trades on a ~1.8-month single-regime window, the winner of a ~90-run sweep with no multiplicity control, and its PRECEDING period was **−17.4% with 0% profitable runs** — regime fragility is the KNOWN failure mode; a bleed on a tape turn is the documented expectation, not a surprise. Probe: `core/agents/tsmom_probe_agent.py`, arms `tsmom_20d_1h_v1`/`tsmom_20d_4h_v1` logged + scored separately, frozen score tanh(\|mom_20d\|/0.10) never re-tuned post-outcome. Expectation is NO-PROMOTE; promotion (only if forward evidence surprises) via frozen `core/promotion_gate.py` on ≥30 RESOLVED forward events PER ARM + explicit owner sign-off | 2026-07-11 |
| Event-driven unlock-short (pre-unlock cliff ≥10%-of-mcap non-insider, W1 entry T−28d / W2 entry T−14d, exit at unlock T, capital-scaled 3%/12% UNLEVERED) | CONFIRMED-GO **strictly as a log-only shadow probe** (`core/agents/unlock_short_probe_agent.py`; audit `_workspace/strategy_pipeline/09_audit_candidate2_final.md`, 6 binding conditions). Fragility recorded UP-FRONT so a future bleed is never re-litigated as a surprise: **100% of backtested profit came from 2025-26 bear-tape events — 2023 was net-NEGATIVE in both arms (F2)** — and the screen's n=32/36 is really **~19/22 independent bets** (SUI/GUN near-contiguous monthly-cliff pseudo-replication, F1). If a bear-to-bull tape transition makes this probe bleed, that is the KNOWN failure mode playing out. W2 is the robustness-primary arm (F3); W3 (T→T+3d post-unlock) failed its AUC gate (0.591<0.60) and is NOT probed. The frozen entry score is expected to FAIL the forward AUC≥0.60 gate (F4) — a legitimate NO-PROMOTE outcome; never re-tune it post-outcome. Promotion ONLY via frozen `core/promotion_gate.py` on ≥30 RESOLVED forward events PER ARM + explicit owner sign-off (~months at 1-3 qualifying events/month — by design). [2026-07-16 evidence update] Independent practitioner backtest (Tigro Blanc, Apr 2026, 22 trades, single-source) replicates the effect (CAR −15.05%, WR 0.773); its full-size −86.6% maxDD blow-up externally validates the 3%/12% caps. [2026-07-23 evidence update] Tokenomist/unlocks.app 236-event matched-peer study (Jun 2024→Mar 2026, BTC-adjusted, placebo-controlled): pre-unlock month drift −14.7% (p<0.001) supports the W1/W2 pre-event entry design; the causal effect concentrates in NEWLY-LISTED tokens (−16.02%, p=0.03; age-matched ≤120d −14.8%, p=0.001) while ESTABLISHED tokens show none (−2.57%, p=0.42) — young-token conditioning is a candidate W-variant prereg only AFTER current arms resolve ≥30 events; never retro-fit the frozen probe. De-dup note: the Medium/MEXC "token_unlock_dilution" backtest carries identical numbers to the Tigro Blanc row (CAR −15.05%, WR 0.773) — republication, NOT independent corroboration. [2026-08-03 evidence update — adversarially verified, 3-vote deep-research round] Effect-size correction with figure labeling: the tokenomist matched-peer CAUSAL estimate is **−4.85%/mo vs BTC (n=221, p=0.02)** — ~3.4× smaller than the raw −16.26% median; the −14.7% pre-unlock drift and −16.26% median cited above are RAW/BTC-relative-unadjusted figures, NOT causal effects. 236→221 event attrition is unexplained by the source (flagged, not assumed benign). The young-token concentration claim (−16.02% early-stage vs −2.57% established) **FAILED 3-vote verification (1-2 as stated)** — the W-variant candidacy above now rests on weakened evidence; it stays deferred (≥30 resolved events per arm) AND requires fresh verification of the age-conditioning claim at prereg time. Keyrock 16k-event timing rules (exit T−30d/re-enter T+14d; 90%-negative; ecosystem-positive) all REFUTED 0-3; only "team unlocks worst (~−25%)" survived (2-1, weak). Net: the ~3.4×-smaller causal effect and refuted conditioning MATERIALLY STRENGTHEN the standing NO-PROMOTE expectation; no probe change. | 2026-07-11 |
| Pullback-momentum MA20/RSI14 — the owner's stated rules (SMA50>SMA200 trend filter; entry close>SMA20 ∧ RSI14 crossing ↑55; exits RSI14>70 or close<SMA20; stop entry−1.5×ATR14; 1%-risk sizing; 4h bybit, 43-sym spec universe) | **Owner-directed forward paper probe — NOT a pipeline GO; the reopen bar is NOT met.** The family stays in the Refuted table below (textbook trend/breakout 0/40 OOS 2026-06-13; RSI mean-reversion NO_EDGE 2026-06; indicator-confluence stacks refuted 2026-06-08) — this row does NOT reopen it. Same-day external sweep (2026-07-22) found no reopen-bar-qualifying evidence and one adverse anchor recorded UP-FRONT so a future bleed is never re-litigated as a surprise: the strongest recent multiplicity-controlled study (Bysik & Ślepaczuk, arXiv 2606.00060, May 2026 — BTC hourly 2018-26, 27-fold walk-forward, 10bps costs, Holm-corrected block bootstrap) shows the momentum baseline at **+31.16% gross → −45.93% net** — cost-death from trade frequency is the documented expectation. Probe: `core/agents/pullback_momentum_probe_agent.py`, arm `pullback_ma20_rsi14_4h_v1`, log-only, frozen score tanh((RSI14−55)/15) never re-tuned post-outcome, 42-bar bounded resolution. Expectation is NO-PROMOTE; promotion (only if forward evidence surprises) via frozen `core/promotion_gate.py` on ≥30 RESOLVED forward events + explicit owner sign-off. Probe exists because the owner directed paper-trading his stated rules ("use what I have said", 2026-07-22) — a log-only forward test is the honest instrument | 2026-07-22 |

## Refuted (do not re-propose without the reopen bar)
| Family | Verdict evidence | Date |
|---|---|---|
| RSI mean-reversion (any timeframe) | 5 coins × 3yr screen, NO_EDGE | 2026-06 |
| Textbook trend/breakout (supertrend, Dow swing, Asian range, BB squeeze) | 8 strategies × 5 majors, independent toolchain, 0/40 OOS | 2026-06-13 |
| Candlestick patterns | 1,989 tests, 0 survivors | 2026-06-07 |
| Indicator-confluence stacks | refuted on own data | 2026-06-08 |
| Kalman/cointegration pairs | 435 pairs, 0 FDR survivors | 2026-06-06 |
| Directional funding-rate signals | IR 0.248, NO_EDGE | 2026-05-26 |
| Formulaic alphas | 443+ tested, best IR≈0.45 pre-cost | 2026-05-25 |
| ML price forecasters (Kronos family) | −EV both directions; 89% alpha loss to costs | 2026-05-29 |
| Hour-of-day / seasonality sweet spots | 0 survive OOS | 2026-06-02 |
| BTC-dominance & ETF-flow timing | NO_EDGE | 2026-06-07 |
| $1–2 scalping | no edge after costs; exits can't manufacture EV | 2026-05-28 |
| Grid / DCA | grid backtest-only; live DCA hard-OFF | standing |
| OI-divergence | NO_EDGE → gates disabled | 2026-05-30 |
| Long-only TSMOM | halves drawdown, no profit edge; implemented default-OFF | 2026-06-15 |
| Post-listing perp short — equal-notional unhedged full-stake sizing ONLY | Signal itself NOT refuted (WR 0.75–0.81, DSR≈1, PBO 0.09, beats control, OOS-WR 0.82–0.88 on 88 funding-charged Binance listings) but fails the capital-preservation MC maxDD p95 ≤0.25 gate at all horizons (2.3–3.3 per-stake tails when listing clusters pump); audit-confirmed 2026-07-09. The capital-scaled 3%/12%-cap variant separately earned CONFIRMED_GO (rev3, unlevered log-only shadow probe) — not covered by this row | 2026-07-09 |
| Cross-venue funding dispersion — binance∩bybit hold-until-sign-flip, taker | Audit-A1-corrected single-round-trip hold model on multi-year data (15 coins, 2,788–4,832 OOS settlements/pair): positive OOS mean carry (best pair ZEC +0.45 bps/settle, DSR 0.9998, MC P(>0)=1.0) BUT per-settlement OOS-WR 0.378 < the frozen 0.55 floor — the mean is a fat-right-tail artifact of a minority of large-differential settlements, not a repeatable per-settlement edge; audit-confirmed. [2026-07-16] MDPI Two-Tiered now published (Mathematics 14(2):346, Jan 2026): only ~40% of top cross-venue spreads are net-positive after costs — external anchor for this NO_GO | 2026-07-09 |
| Funding-settlement-window entry-timing refinement (F1, −15m/−30m/−60m/+15m offsets vs the live [20,180]min pre-settlement window) | Real binance+bybit 15m premium-index series (30/30 series, 2,586 pooled qualifying settlements, both venues ≥120 min floor). Best offset (−15m) mean +1.33 bps/episode fails the 5bps unmodeled-fill bound; OOS-WR 0.510 < 0.55 floor; fold sign unstable ([+,+,+,+,−]); venue sign unstable (binance +2.37bps vs bybit −0.48bps). F1's current entry window is already near-optimal; +15m post-settlement costs −11 to −15bps | 2026-07-11 |
| Quarterly-futures basis leg-swap (ETH, conditional on APR_basis > APR_funding at entry, express F1 short leg via dated future instead of perp) | Conditional mean +9.2bps/event clears DSR(0.463)/PBO(0.0)/OOS-WR(0.625)/maxDD(0.114) but MC P(total>0)=0.683 fails the 0.95 capital-preservation floor; unconditional mean −7.3bps/event; n=31 (bare minimum, 64 monthly decision points 2021-03→2026-06). BTC variant stays INFEASIBLE-AT-CURRENT-CAPITAL (unchanged, not re-probed). [2026-07-16] AEA 2026 abstract: quarterlies fall 8–10% vs spot in stress events (perps ~3%) — fixed-leg carry instruments carry MORE stress convexity, not less | 2026-07-11 |
| Band-geometry conditional entry filters (positive selection) | 0/16 pre-registered buckets GO — ALL fail after-cost expectancy >0 (best subset f6 small-gap 0.15-0.30% EMA: 78.5% WR at −$0.06/trade, and it fails the ≥10% flow floor at 6.4%); 12-day shadow sample (14,555 resolved band outcomes), Bonferroni m=16 (`_workspace/strategy_pipeline/13_band_conditional_screen.json`). Two buckets INSUFFICIENT_DATA pending forward backfills: f4 spread-percentile (FeatureVector writer dormant since 2026-06-14) and f5 fill_type+MFE/MAE (accrue forward only, needs ≥300 trades). NEGATIVE selection is separately shipped as the flag-gated band-lane-only veto `BAND_REGIME_FILTER_ENABLED` (4h ADX>30 / BTC-vol-ratio<0.7) — WR-band protection + bleed reduction, NOT edge | 2026-07-12 |
| F1 funding-percentile persistence selectivity (P75/270-settle + persistence entry overlay, trailing-median decay exit) | Structural: forfeits 79% of total carry harvest (0.21× vs 0.75× guard) and flips negative under 2× cost stress while the incumbent stays robustly positive; +2.16 bps/settle efficiency delta not distinguishable from zero under coin-clustered bootstrap; efficiency sub-claim UNTESTABLE in the 2025–26 compressed-funding regime (qualifying episodes structurally absent). 3-model debate CONFIRMED_NO_GO (`_workspace/strategy_pipeline/16_debate_15b_f1_selectivity.md`); entry-only variant = NEW prereg only (fixed empty-fold semantics + clustered delta bootstrap required) | 2026-07-17 |
| Stablecoin-peg mean-reversion — long-discount spot, absolute threshold θ=10–20bps, Binance, 48h timeout, zero-fee era | θ=10 (n=75): mean −8.3 bps/event, WR 0.067, timeout-dominated (FDUSD persistent ~0.9978 sub-peg = regime, not events; 19/24 trigger months); θ=20 (n=32): −29 bps, MC maxDD p95 0.257 (Apr-2025 FDUSD crash −8.27% through gapped stop). Structural kill: wherever mean>0 under generous costs, frozen-score AUC 0.548–0.554 fails even at ZERO cost. NOT covered (NEW prereg only): θ≥30 (n=11), USDC-only, transient/regime-relative event definitions, maker/longer timeouts, other venues. 3-model debate CONFIRMED_NO_GO (`16_debate_15c_depeg.md` rev2) | 2026-07-17 |
| Staked-asset wrapper discount (wBETH/stETH vs ETH) — CEX-spot taker round-trip, 1h-close basis, perp-hedged | Stage-1 stop rule: p95 discount 21.3 bps (WBETH) / 27.9 bps (STETH) vs 49.96 bps 4-leg floor (fees-only zero-slip 30 bps floor also unmet); ≥20 bps episodes median duration 1h — transient, not warehousable; funding credit negligible. Numbers reproduced bit-exact in 3-model debate CONFIRMED_NO_GO (`16_debate_15d_wrapper.md`). Sub-hourly, resting-limit, and on-chain-redemption expressions untested = NEW candidates | 2026-07-17 |
| CFTC asset-manager options-pressure → BTCUSDT perp (TFF Combined−FutOnly options-only Δ, residualized vs contemporaneous+1–8w BTC returns, Fri 15:30 ET release lag, H1/H2/H3 × 2 directions, CME BTC 133741) | Dual-agreed SCREEN (18_*) expectation NO_GO confirmed: 0/6 residual variants pass frozen gates. Best cell H2_long_on_pos (n_OOS=69): mean +1.0%, WR 0.609, DSR 0.532, OOS-WR 0.577 BUT MC P(total>0)=0.8855<0.95 and maxDD p95=0.776≫0.25 — capital-preservation kill. Delta-drift kill did not fire; joint PBO≈0.10. Artifacts `20_prereg/screen/audit_c1_*`; harvest `data/cftc_cot/` (gitignored). NOT covered: micro-BTC primary, conditioned high-downside-risk cell with separate prereg, maker-only fills | 2026-07-23 |
| AccBand frac dual-goal (WR∈[0.59,0.67] ∧ BE_WR≤0.59 ∧ EV>0 ∧ PF>1 on MCP directional geometry) | CONFIRMED_NO_GO: frozen prereg `30_prereg_accband_frac_dual_goal` (sha256_md `f5de9f0a…`); after-cost geometry sim n≈8700 (`05_accuracy_band_sim.md`) — **0/12 cells** clear joint gates; every frac has expectancy≈−0.24R and BE_WR≥0.68 (G080 extrapolated still BE≈0.68, WR≈0.54). WR-in-band and after-cost profit are mutually exclusive on the measured no-edge path. Artifacts `30_screen_accband_frac_dual_goal.{md,json}`; screen script `research/screen_accband_frac_dual_goal.py`. AccBand remains WR-research geometry only; profit requires separate evidence lanes | 2026-07-24 |
| VPIN jump-risk veto overlay on AccBand (raw θ∈{0.55,0.60,0.65,0.70}, N=50 volume-clock, BTC/ETH Binance USDT-M aggTrades) | CONFIRMED_NO_GO under owner Stage-0 override: prereg `27_prereg_vpin_jump_veto` (sha256_md `2b880d1b…`); substrate 3,050 AccBand binance BTC/ETH resolved R rows (13_band); VPIN coverage 100% but **mean VPIN≈0.127 → fire_rate=0 at all θ**; ΔR=0; MC P(>0)=0 on kept=baseline. Bear R4 (θ miscalibration) confirmed. Artifacts `27_screen/audit/integration_vpin_*`; harvest `data/aggtrades_vpin/` (gitignored). Directional VPIN remains STOP. Reopen only via NEW hashed prereg (e.g. CDF/percentile θ) — never silent θ edit | 2026-07-25 |
| Clamp-print zero-information null (F1 telemetry MEASUREMENT — funding prints equal to the regime-scaled venue baseline) | Frozen prereg `39_prereg_clamp_print_information_v2` (sha256 `dda32c8cf71d…`, committed pre-outcome 4be03ad; supersedes burned 38_ which pre-computed outcomes); corrected (venue,symbol)-linked CMH run statistically FALSIFIES the formal zero-excess null in **9/9 testable cells** (Bonferroni α=0.05/12; OR_MH 1.55–7.83; clamp next-sign 0.88–0.96 vs matched positive controls 0.58–0.90); 3 cells INSUFFICIENT_DATA (bitget-1h/2h absent, binance-2h zero informative strata). Dual-model both-agree (`39_verdict_reconciled`): mechanism is **sticky venue default-state persistence, NOT positioning information** — the floor predicts itself; the practical zero-information claim is SUPPORTED. Round-1 Codex INVALID_RUN (cross-venue outcome leak, 66.9% of rows) fixed + regression-tested before the counted rerun. **No trade use; no F1 change; clamp-aware filter = log-only telemetry ONLY; any decision use requires its own NEW prereg** | 2026-07-28 |

| Quarter-hour clock-boundary opening order-imbalance → 4–12h drift (Binance USDT-M majors, taker, residualized vs ret_1h/ret_4h/log_vol_1h) | CONFIRMED_NO_GO: prereg `22_prereg_c3_quarter_hour_imbalance` (sha256 `7b33c639…`) frozen pre-outcome (10:30:00Z, 43 min BEFORE the 11:13:55Z screen); BTC+ETH 2026-Q2 pilot, 8,736 events/symbol (manifest `d3b5632d…`); **0/6 residual variants pass** — aligned OOS means H4 −23.3 / H8 −33.0 / H12 −18.5 bps after cost, WR 0.38–0.47 < 0.55 floor, MC pass False all cells, DSR ≤0.003; +20 bps expansion bar missed by ~38 bps; 6-symbol/2025–26 expansion NOT authorized. **The "punitive cost model" defence is dead** (ai-reviewer 2026-07-26): the aligned/contrarian midpoint is exactly −20.0000 bps at all three horizons = the symmetric 2×(5bps fee+5bps slip) charge; stripping cost out leaves gross alpha H4 −3.26 / H8 −13.03 / H12 +1.45 bps — sign-scattered, no detectable signal — so **even at ZERO fee and ZERO slippage the best cell is 13.8× short of the bar**. External: Shynkevich JFM 2026 46(5):904–930 (peer-reviewed — boundaries carry elevated transaction costs) corroborates; arXiv 2607.09426 fails the reopen bar (no costs, no multiplicity, §6 tables unretrievable) despite an asserted independent replication. NOT covered (NEW prereg only): XRP/SOL/DOGE/ADA, maker-first boundary execution, cross-venue, sub-second. A no-retune forward Q3+ extension of the SAME frozen spec is the only conceivable re-measure and is **explicitly NOT authorized**. Artifacts `22_*`, `30_review_c3_quarter_hour.md`, `37_*` | 2026-07-23 |
| Micro Two-Sided Inventory (MTSI) — AS reservation quotes, ≤$1 gross inventory, mild microprice tilt, maker fees + 0.5×half-spread adverse-selection haircut, SPOT+FUTURES synthetic (bybit fee tables) | CONFIRMED_NO_GO: frozen prereg `55_prereg_mtsi_inventory` (sha256_md `5262e070…`); offline sim `research/sim_mtsi_inventory.py` — **0/6 cells** clear joint gates; n_clips 840–1995/cell but mean clip PnL negative and PF below 1.0 on all cells (fees+AS dominate micro spread capture). Inventory and clip-size invariants held (max \|q\|≤$1). Expectation met. **No shadow probe** (GO required). Reopen only via NEW hashed prereg (e.g. rebate venue, real tape, different AS haircut) — never raise $1 cap or reopen allowlist from this row. Artifacts `55_deep_research_*`, `55_screen_mtsi_inventory.*`, `55_integration_report_mtsi.md`. **EVIDENCE-LOSS NOTE (2026-08-04):** those `55_*` workspace paths were local-only / not git-tracked at annotation time — treat citations as best-effort until committed. AccBand κ (`52_*`) remains deferred secondary | 2026-08-01 |
| TP-exit geometry as the fix for MCP directional PAPER expectancy (TP = k × SL_distance, k∈{0.35…3.0}, replayed on realized MFE/MAE) | CONFIRMED_NO_GO: frozen prereg `68_prereg_tp_geometry_replay` (sha256 `4a848c84d4d7…`, committed 67b46f1 **before** outcomes; screen `research/screen_tp_geometry_replay.py` re-verifies the hash at startup and refuses to run if edited). n=275 closed PAPER trades with MFE/MAE + entry_stop_px; **0/10 arms positive**, every Bonferroni-corrected CI (α=0.005) excluding zero on the wrong side. Best arm k=0.45 mean −0.002557/trade, needing +0.000955 just to reach non-significance. **The finding in one line: k=0.35 hits TP on 172/275 (62.5% WR) and still loses** — geometry manufacturing WR, not edge. Monotone decay as k rises (fewer TP hits, same 70 stops) ⇒ no optimum inside OR outside the grid. **Strength: the replay is OPTIMISTIC by construction** (MFE/MAE are path-agnostic, so a trade that nearly stopped out before running to its MFE books as a clean win) — failing under a model tilted toward H1 is strong evidence for H0. Root cause (exploratory, NOT pre-registered): **mean MFE 0.435% vs mean MAE 0.523%, ratio 0.831** — trades move against the position ~20% more than for it, so no stop/target arrangement can profit. Corroborates the same-day diagnostic (`reports/profitability_diagnostic_2026-08-12.md`): gross −37.19 pre-fee over 279 trades; corr(mcp_score, realized_pnl) −0.019 at n=1,170. Closes the "entries are fine, only the exit is wrong" hypothesis. NOT covered (NEW prereg only): per-bar OHLCV path-aware replay, trailing/partial exits, time-varying k, entry-signal changes. **No runtime flag changed** (prereg §7 forbids it) | 2026-08-12 |
| Hourly L2 top-of-book depth-imbalance → forward drift (15 symbols, θ∈{0.2,0.3}, H∈{1,4}h, de-overlapped, 22bps taker RT) | CONFIRMED_NO_GO: frozen prereg `70_prereg_l2_imbalance_drift` (sha256 `88ca0168…`, committed 5c847b8 BEFORE outcomes; screen `research/screen_l2_imbalance_drift.py` verifies the hash at startup). 53d harvested feed (`data/l2_history.jsonl`, 24,675 obs): **0/4 testable cells positive** — pooled-15 means −22.7 to −33.1 bps after cost ⇒ gross ≈ 0 to −11 bps, WR 0.27–0.38, Holm p=1.0, MC p=0.00; signed book pressure carries no directional information at hourly aggregation (mirrors the refuted quarter-hour TRADE-imbalance zero-gross finding). Majors-only cells INSUFFICIENT_DATA (n 7–91 < 100). NOT covered (NEW prereg only): sub-hourly/tick-level imbalance, deeper book levels, maker execution, imbalance-CHANGE signals | 2026-08-13 |
| Deribit rr25 skew-shock → BTC/ETH perp drift (Δrr25 ≥ {0.5,1.0} pts hourly, H∈{4,12}h, de-overlapped, 22bps taker RT) | CONFIRMED_NO_GO: frozen prereg `71_prereg_skew_shock_drift` (sha256 `30953f5a…`, committed 630d9d1 BEFORE outcomes; screen `research/screen_skew_shock_drift.py` verifies the hash at startup). 53d harvested feed (`data/skew_history.jsonl`): **0/7 testable cells positive** — aligned means −5.8 to −61.2 bps after cost, Holm p=1.0 all cells, and **both aligned AND contrarian negative in 6/7 cells** ⇒ no directional information in either sign (the reported-not-gated contrarian column caught this). 1 cell INSUFFICIENT_DATA (ETH θ=1.0 H=12, n=27<30). NOT covered (NEW prereg only): sub-hourly shocks, rr25 LEVEL/percentile signals, atm_iv shocks, expiry-conditioned windows | 2026-08-13 |
| LLM multi-agent debate frameworks on the trade path — TradingAgents (TauricResearch, 98.3k★, UCLA/MIT arXiv 2412.20138) and the analyst→bull/bear-debate→trader→risk-manager topology generally | **DOES NOT MEET THE REOPEN BAR — evidence audited 2026-08-16; adopt nothing into the decision path.** Headline claims are Sharpe **8.21/6.39/5.60**, 23.21% cumulative, maxDD 0.91–2.11% — but the full paper (arXiv 2412.20138v5) shows the window is **Jan 1–Mar 29 2024 (3 months) on 5–6 correlated US mega-cap tech stocks**, with **NO transaction costs, fees, slippage or commissions modeled anywhere**, **no out-of-sample or walk-forward validation**, **no multiplicity control**, and **no Limitations section**; the authors' own footnote attributes the "exceptionally high SR" to **"few pullbacks"** in that window, and the repo self-describes as "a research scaffold… not a strategy with a fixed, replicable return", **never deployed live** ("future work"). Fails all four reopen criteria independently; zero cost modeling alone is disqualifying against our ~22bps round trip. **Local evidence already refutes the family at 20× the sample**: our own LLM-influenced score measured corr(score, PnL) = **−0.019 at n=1,170** with costs, and LLM/sentiment was removed from the trade path 2026-08-04 (De-Emotion). Independent live benchmark (arXiv 2510.11695) finds agent ARCHITECTURE dominates model choice, and a comparable framework's TSLA return swung **−38.72% → +21.91% purely on an LLM-backbone swap** — noise wearing a rationale. **BINDING STRUCTURAL OBJECTION (council 2026-08-16, 4/4):** an LLM step in the decision path **destroys falsifiability** — all ~2,400 refutations were run against a deterministic, replayable path, and nothing downstream of a non-replayable step could ever enter this ledger again. Also: the topology already exists here (features → meta-filter → risk_manager → order_manager); it is not crypto-shaped (Yahoo/StockTwits/FRED/SPY, no funding, no 24/7, no perp liquidation); and cost scales with CANDIDATES screened (49,384–162,742 gate checks/lane), i.e. a five-figure inference bill on a $5,000 account. **A pre-entry LLM veto is NOT a cheap subset — it is the charter boundary and needs its own prereg.** Re-backtesting the debate on crypto is uninterpretable by construction (model cutoffs postdate any testable window); only forward-only testing is honest. NOT covered: non-decision engineering (append-only decision logging) — permitted, but never anything that RESTORES gate/latch values on resume | 2026-08-16 |

## Open (INSUFFICIENT_DATA — not refuted, not screenable yet)
| Family | Status + reopen condition | Date |
|---|---|---|
| Delisting forced-flow (2a capital-scaled perp short on announcement; 2b survivor-venue spot bounce) | n=34 independent Binance delisting events 2022-03→2026-06 (near the structural ceiling) < frozen 30-event floor per covered variant — fail-closed, NO ledger-refutation. Adverse point evidence recorded: ALPACA 2025-05 squeeze −22.4× stake incl. −96%-of-notional funding (reproduced 3× independently); funding negative on 21/29 covered legs; 2b bounce negative even at true zero cost. Cite `_workspace/strategy_pipeline/16_debate_15a_delisting.md` (4 binding corrections), NOT the 15a screen narrative. Reopen: ≥30 covered events (~1–2 yr forward accrual at 8–12 events/yr) or an isolated-margin/stop-overlay NEW pre-registration (debate showed isolated-margin E2 passes MC point estimates but still fails mean-vs-control + DSR) | 2026-07-17 |

## Reopen Bar
Peer-reviewed or equivalently rigorous 2025+ evidence with genuine out-of-sample validation, FDR/DSR-grade multiplicity control, and after-cost accounting on liquid crypto at retail venues. Quote it verbatim. Meeting the bar earns a SCREEN (via `strategy-evidence-pipeline`) — never a build.

### Reopen tests run (cite these instead of re-searching)
| Date | Tested | Result |
|---|---|---|
| 2026-07-25 | MA/RSI/ATR pullback-momentum family **and** the "sweep thousands of variants → deploy the strongest survivors" methodology (owner-proposed) | **BOTH STAY REFUTED.** No source meets the conjunctive bar; no documented case of a survivor-of-thousands sweep holding up in live crypto was found. Independently replicated: the 2026-07-23 pass (`_workspace/strategy_pipeline/29_deep_research_pullback_ma20_rsi_2026-07-23.md`) reached the same STOP verdict from a **disjoint source set** (arXiv 2606.00060: momentum +31.16% gross → −45.93% net at 10bps, 27-fold walk-forward, Holm-corrected) — and notes the owner's *level*-based `RSI>55` re-fires every bar vs the probe's *event*-based cross↑55, i.e. strictly higher turnover than the already-losing probe. Q2 falsified three ways: **(a) [3-0 verified]** IJFE 10.1002/ijfe.2863 executed the exact plan — ranked ~7,851 variants, took top 15, added Harvey-Liu + Hansen SPA + genuine OOS → **14/15 failed**, despite in-sample Sharpe up to 1.350; **(b) [verification incomplete — session limits, not disputed]** Bailey/Borwein/López de Prado/Zhu (SSRN 2326253): 8,800 variants over a **pure random walk** → in-sample Sharpe 1.27 passing significance (PSR-Stat 2.83) yet **PBO 55%**, ~53% of OOS Sharpes negative — filtering on Sharpe/WR/PF cannot separate artifact from edge; (c) DSR: sweeps are *guaranteed* to produce profitable-looking rules in random data, the rejection threshold **rises with trial count**, and hold-out fails after ~20 applications. Q1: the 7,846-rule × 12-coin × with-costs STW study finds "traditional technical analysis rules have no value in cryptocurrencies" (scope: no oscillator/ATR legs); the strongest counterweight (Hudson-Urquhart, 14,919 rules) fails after-cost (zero-cost significance tests), failed its own OOS on **both BTC series**, and its RSI rules are contrarian-short — opposite polarity to a long RSI-cross entry. Converges with live probe #7 (`pullback_ma20_rsi14_4h_v1`, zero selection applied): 12 resolved / 0 wins, mean −0.728R, 0 of 12 exits via the RSI>70 profit path; 95% upper bound on true WR = 22.1%. Full report: `reports/deep-research_pullback-momentum-reopen-bar_2026-07-25.md` |

## Usage
- Reject refuted proposals by citing the ledger row — conversationally, without launching workflows.
- Route genuinely new ideas to the `strategy-evidence-pipeline` skill: screen → shadow → promotion gate. Nothing is wired live from research alone.
- When a new screen concludes NO_GO, ADD a row here (family, evidence, date) so the knowledge compounds.
