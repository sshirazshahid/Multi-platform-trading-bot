1. **VERDICT: INVALID_RUN — no confirmed falsification.** The prereg hash, 12-cell alpha, Stage-0 threshold, and CMH formula are faithful. But the construction is not: “next print” is grouped by `symbol` alone, not `(venue, symbol)`. Since 136 symbols occur on multiple venues, 66.9% of eligible rows link to another venue and 32.3% link at the same timestamp—not a symbol’s own *next* settlement. This invalidates the displayed 9-cell result; fix and rerun under the same frozen prereg.

2. **INTERPRETATION:** Even if correctly rerun, OR>1 would show persistence in the *published funding-sign state*, not positioning information. A positive venue-default/baseline print can mechanically repeat when the venue’s default state is sticky; non-clamp positives are deviations and need not persist. “Is the next print positive?” cannot distinguish default-state persistence from informative positioning. The prereg’s 1h volatility-persistence warning reinforces this.

3. **OPERATIONAL DISPOSITION:** No substantive ledger refutation row yet. At most: “Clamp-print v2 — INVALID_RUN: cross-venue/same-time outcome linkage; rerun required; no use.” A clamp-aware F1 telemetry filter is **less justified as a decision filter** after this result: retain it only as a log-only diagnostic until a valid rerun and, if anomalous, its own preregistered screen.

4. **Pre-refuted trade implication:** Do not infer “positive clamp means take/keep F1,” “front-run the next funding,” or any directional long/short signal. This measures neither price returns nor net carry after costs, and the apparent effect may be a sticky reporting default.
