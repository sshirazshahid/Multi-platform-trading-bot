# 18 — Run Context (2026-07-22): Dual-model per-pair FUTURES verdicts

**Owner request (verbatim):** "Can you integrate both Claude-Fable-5 and Codex-Sol-5.6? Both will /deep-research and refine strategies then simulate and test them. Argue with each other and then bring a final verdict on each pair in FUTURES trading."

**Phase 0 findings:**
- Codex CLI available again (usage-limit probe returned CODEX-OK; model gpt-5.6-sol). Dual-model design feasible.
- Workspace numbering continues at 18_ (convention since run 14; ledger rows cite in-place paths).
- Verdict universe = `data/strategy_specs/MCP_DIRECTIONAL_PAPER.json` `symbols` (~44 USDT-perps, 3 venues).
- Model-policy deviation (owner-directed, this run only): Codex GPT-5.6-Sol joins as an independent research + verdict voice; Fable remains orchestrator and all Agent-tool calls.

**Honesty frame (binding, from the ledger):** no directional futures EDGE exists on any pair by every prior measured screen (~2,400+ refuted tests; band lane is WR-geometry not edge, expectancy last measured ≈ −0.24R; F1 carry validated but regime-idle). Therefore "verdict per pair" here means: fitness of each pair for the aggressive-PAPER band program + any evidence-backed strategy note (probe membership, data gaps, cost/liquidity, incident history) — NOT a manufactured per-pair edge claim. Any genuinely novel candidate from either model goes through the standard pre-registered after-cost screen before any GO.

**Plan:**
- Phase 1 (parallel research): A = Fable scout (delta since the 2026-07-17 sweep only, max 2 candidates); B = Codex scout (same task via codex exec, ledger read first) → 18_scout_fable.md / 18_scout_codex.md
- Phase 2 (simulate/test): C = per-pair evidence dossier from LOCAL data (warehouse band cohort, 13_band_outcome_cache per-symbol outcomes, funding/ohlcv coverage, probe membership, quarantine flags) → 18_pair_dossier.{json,md}; novel candidates (if any clear the ledger) → standard screens 18x_screen_*
- Phase 3 (argue): D = Codex per-pair verdicts from dossier; E = Fable per-pair verdicts (independent); F = reconcile with one rebuttal round each on disagreements → 18_debate_pair_verdicts.md
- Phase 4: owner verdict table + changelog row.
