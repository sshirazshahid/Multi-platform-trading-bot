1. CONFIRM. The corrected `(venue, symbol)` outcome linkage is faithful to the frozen prereg; hash/commit linkage holds and the focused suite passes (12/12).

2. Venue default-state persistence—not positioning information.

3. Endorsed ledger row:

| Family/finding | Evidence | Date |
|---|---|---|
| Clamp-print zero-information null (F1 telemetry measurement) | Frozen prereg `39_` (SHA-256 `dda32c8cf71d…`, committed pre-outcome in `4be03ad`); corrected venue+symbol CMH run statistically FALSIFIES the formal zero-excess null in 9/9 testable cells (Bonferroni α=0.05/12; OR_MH 1.55–7.83); 3 cells INSUFFICIENT_DATA. Mechanism is sticky venue default-state/sign persistence, **not** incremental positioning information. No trade use and no F1 change follows; any decision use requires a separate new prereg. | 2026-07-28 |

4. CONFIRM: a clamp-aware filter is justified only as log-only telemetry diagnostic; any decision use requires its own new prereg.
