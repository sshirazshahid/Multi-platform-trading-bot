# Decision funnel 27/7943 — honesty audit (2026-07-30)

**Question:** Why does Mission Control show ~27 ALLOW of ~7943 scored?  
**Verdict:** The counter is correct for a ~6h quiet-regime SCALP_MODE window. The actionable bugs were (1) mislabeled score-floor SKIPs and (2) TradFi (MSFT) false ALLOWs under AccBand — **not** an ATR floor that needs loosening.

*Sources: N≈8 web + local warehouse/Mission Control. Confidence: High on local measurement; Medium on external after-cost consensus (Firecrawl/Exa MCP unavailable — WebSearch used).*

## Executive summary

In a ~6h window the warehouse showed ~7969 scored candidates with **27 ALLOW / ~7942 SKIP**. Dominant SKIP families were `scalp_req_fail` and `scalp_veto:quiet` (ATR &lt; 0.8%). Separately, ~1953 SKIPs were bare `vwap_near=…|rsi=…` strings (4/4 required passed, score &lt; floor) that Mission Control could not family-aggregate. All sampled ALLOWs were **MSFT/USDT** (TradFi), not crypto. External evidence and this bot’s ledger agree: retail scalping after fees is typically −EV; funding/basis carry is the durable class. **Do not** lower `SCALP_MIN_ATR`, disable `SCALP_MODE`, or reopen AccBand/`paper_fallback` without a new hashed prereg.

## 1. Local measurement (ground truth)

| Window | Scored | ALLOW | Notes |
|--------|--------|-------|-------|
| 60m | ~1326 | 0 | Fully quiet |
| **6h** | **~7969** | **27** | Matches Mission Control |
| 24h | ~28k | ~1876 | Higher when ATR clears |

6h SKIP family breakdown (approx.):
- `scalp_req_fail` ~4311
- bare indicator strings (score-below-floor, unlabeled) ~1953
- `scalp_veto:quiet` ~1300
- `scalp_veto:ranging` ~353

ALLOW sample: **26/26 MSFT** with no terminal `decision_events` row joined — AccBand funnel looked “alive” on tokenized equity noise.

## 2. Why the scorer rejects so much

[`SCALP_MODE.enabled`](config.py) defaults **true** → [`_score_coin_scalp`](core/mcp_brain.py):
- veto ATR_1h &lt; 0.8%
- veto 4h ADX &lt; 15
- require 4/4 (VWAP near, RSI 40–60, ADX≥20, ATR≥0.8%)
- then EntryFloor (66 under current env) + layers

Quiet markets correctly fail these gates. That is **protective idle**, not a broken Mission Control cascade (stage join on `candidate_id` still closes).

## 3. External evidence (after-cost)

- Funding / cash-and-carry remains the structural retail-viable class when net funding clears all-in costs; rate-flip and fees dominate failure modes ([Kraken funding arb](https://www.kraken.com/learn/futures-trading-funding-rate-arbitrage); [BackQuant basis](https://www.backquant.com/learn/basis-trade); Bitsgap Q2 2026 funding regime notes).
- Retail scalping / short-horizon TA often prints high WR in backtests then goes −EV after fees/slippage ([StratProof 22 strategies](https://stratproof.com/blog/paper-trading-22-strategies-real-fees); Bybit AI scalping after-cost writeup; WEEX scalping cost floor guidance).
- ATR as a *minimum* activity filter is standard risk hygiene (CoinQuant ATR filter literature) — lowering it to force ALLOWs fights the cost floor.

Aligns with this repo: F1 only when `net_edge` clears; AccBand WR ≠ edge; EconGate=`strict`; Week-2 early call **2026-08-13**.

## 4. What we shipped (honesty, not gate reopen)

1. **`scalp_score_below_floor(score&lt;floor):…`** skip labels when 4/4 pass but score fails ([`core/mcp_brain.py`](core/mcp_brain.py)).
2. **`analysis_only_accband_scope`** SKIP for `ANALYSIS_ONLY_BASES` while AccBand geometry is on (does not flip global `ANALYSIS_ONLY_ENFORCED`).
3. Mission Control: `reason_family` maps legacy bare `vwap_near=` strings; brain `context` exposes scorer_mode / allow_crypto / allow_tradfi / interpretation ([`mission_control/state.py`](mission_control/state.py), [`app.js`](mission_control/static/app.js)).

## 5. Non-actions (binding)

- Do **not** lower `SCALP_MIN_ATR` below 0.8 or disable `SCALP_MODE` without a new hashed prereg + dual-agree screen.
- Do **not** reopen AccBand `paper_fallback` or lower EntryFloor below 66 for “more allows.”
- Do **not** treat Mission Control allow-rate as a KPI for edge — refuse −EV remains success under the approved autoplan.

## 6. Honest next levers

1. F1 carry when `trading_bot_f1_edge_status` leaves `idle_no_edge` for after-cost-positive episodes.
2. Unlock / listing event accrual (Track D) — market events, not scorer looseness.
3. Week-2 owner call **2026-08-13** if unlock+listing+F1 still non-accruing → CONTINUE / PIVOT / STOP.

## Sources

1. Local `data/warehouse.sqlite` candidates + Mission Control `load_brain` (2026-07-30).
2. [Kraken — Funding rate arbitrage](https://www.kraken.com/learn/futures-trading-funding-rate-arbitrage)
3. [BackQuant — Basis trade](https://www.backquant.com/learn/basis-trade)
4. [Bitsgap — BTC hedge funding Q2 2026](https://bitsgap.com/blog/what-a-spot-hedge-on-perpetuals-really-cost-in-2026)
5. [StratProof — 22 strategies after real fees](https://stratproof.com/blog/paper-trading-22-strategies-real-fees)
6. [WEEX — Scalping without getting burned](https://www.weex.com/wiki/article/how-to-build-a-scalping-strategy-without-getting-burned-ms6fj367p00p0xy1papjb3vq)
7. Repo ledger / autoplan 2026-07-30 (F1-only live; AccBand ≠ edge; EconGate=strict)

## Methodology

Sub-questions: (1) is 27/7943 a counter bug? (2) what binds the scorer? (3) are ALLOWs crypto? (4) does external evidence support loosening scalp ATR? Searched web + live warehouse. Firecrawl/Exa MCP not configured — WebSearch only.
