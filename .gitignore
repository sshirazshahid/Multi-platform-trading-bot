# OS-generated files
.DS_Store
Thumbs.db

# Python cache & build artifacts
__pycache__/
.pytest_cache/
*.py[cod]
*.pyo
*.so
*.egg-info/

# Virtual environments
.venv/
venv/
env/

# Secrets & local config (NEVER commit — contains live API keys)
.env
.env.*
!.env.example
.claude/settings.local.json

# Tool configurations
.idea/
.vscode/
.envrc
.mcp.json

# Logs & temp
logs/
*.log
*.tmp

# Jupyter
.ipynb_checkpoints/

# Generated reports
reports/

# Kronos foundation-model eval (weights + cloned source; vendored model code IS committed)
models/kronos/
.kronos_src/
vendor/kronos/**/__pycache__/

# Derivs harvester forward-accumulation store (grows over time; runtime data)
data/derivs_history.jsonl

# Transient agent / tool state (Ruflo swarm, worktrees, vector + agent DBs)
.claude-flow/
.swarm/
.claude/worktrees/
.pytest_tmp/
agentdb.rvf
agentdb.rvf.lock
ruvector.db

# SQLite transient files + backups (never commit — DB internals / large)
data/*.sqlite-shm
data/*.sqlite-wal
data/*.sqlite.bak*
data/*.sqlite-bak*

# Regenerable runtime caches / trained-model artifacts
data/ohlcv_cache/
data/models/

# CI artifacts
htmlcov/
.coverage
# .secrets.baseline is tracked — needed by detect-secrets pre-commit hook

# 2026-06-01: untrack churny runtime state — was causing merge conflicts + 80MB
# warehouse bloat in git. Files remain on disk; the bot maintains them locally.
data/warehouse.sqlite
data/heartbeat.json
data/positions.json
data/positions.json.bak
data/risk_state.json
data/mcp_state.json
data/mcp_decisions.jsonl
data/mcp_accuracy.json
data/execution_stats.json
data/kelly_stats.json
data/post_mortem.json
data/sentiment_history.json
data/news_cache.json
data/news_latest.txt
data/learning_report.json
data/learning_report.html
data/daily_check.json
data/auto_mutations.json
data/exchange_positions.json
data/spot_portfolio.json
data/knowledge_model.json
data/knowledge_model.bak.json
data/calibration.json
data/calibration_pairs.json
data/calibration_iso.pkl
data/virtual_wallet.json
data/wallet_snapshot.json
data/trailing_peaks.json
data/*.runaway-*.bak

# WS3c liquidation forward-harvest (accumulated locally)
data/liquidations_history.jsonl
data/liquidations_status.json
